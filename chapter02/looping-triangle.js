// ===================================================
// My attempt:
// ===================================================
let hash = '#';

while (hash.length <= 7) {
  console.log(hash);
  hash += '#';
};

// ===================================================
// Book's solution:
// ===================================================
for (let line = '#'; line.length < 8; line += '#') {
  console.log(line);
};